import { defineConfig } from 'vite'

export default defineConfig({
  // Use './' for relative paths — works on both GitHub Pages subdirectory
  // and Vercel root. Vercel serves from / so this is fine either way.
  base: './',
})
